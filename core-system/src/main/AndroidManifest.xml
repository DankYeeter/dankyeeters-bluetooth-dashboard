<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Deliberately NO android.permission.INTERNET. This app is fully
      self-contained; nothing is ever sent anywhere.

      WRITE_SECURE_SETTINGS cannot be granted at runtime; the user grants it
      via ADB (or Shizuku). We only declare it so the grant sticks, and detect
      its absence to show instructions.
    -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!--
      BLE scanning for AirPods proximity beacons. neverForLocation: we decode
      battery/wear state from a public advertisement and never derive location,
      so Android must not force the location permission on the user.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <application>
        <!--
          stopWithTask="false" is the whole point: swiping the app out of
          Recents must not stop the equalizer. Only "Force stop" does.

          Type connectedDevice, because that is what this is — the service
          exists to keep a correction attached for a connected Bluetooth
          headphone and to re-apply its profile when it reconnects.
        -->
        <service
            android:name="dev.dankyeeter.btdashboard.system.service.EqForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice"
            android:stopWithTask="false" />

        <!--
          The second, and in practice the more reliable, way the service comes
          up: BOOT_COMPLETED is throttled and can take minutes, while a
          headphone connecting is both immediate and the moment the EQ starts
          mattering. exported="true" because the sender is the system.
        -->
        <receiver
            android:name="dev.dankyeeter.btdashboard.system.service.BluetoothConnectReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="dev.dankyeeter.btdashboard.system.boot.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--
          Session mode (no-privilege fallback): apps that broadcast
          ACTION_OPEN_AUDIO_EFFECT_CONTROL_SESSION let us attach to their
          session. Tidal does not do this reliably — hence the Shizuku path.
        -->
        <!--
          Off by default on purpose. These broadcasts fire on every track
          change of every well-behaved player, and in global-EQ mode (the
          common case) the delivery was dropped on the first line of
          onReceive — after the system had already woken this process for it.
          EqController enables the component only while session mode is
          genuinely in use.
        -->
        <receiver
            android:name="dev.dankyeeter.btdashboard.system.attach.AudioEffectSessionReceiver"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.action.OPEN_AUDIO_EFFECT_CONTROL_SESSION" />
                <action android:name="android.media.action.CLOSE_AUDIO_EFFECT_CONTROL_SESSION" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
