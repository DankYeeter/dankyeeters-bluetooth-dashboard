# PLAN.md targets recent Pixels, but this value is independent of the
# project's compileSdk/targetSdk (36). Robolectric 4.14.1 does not ship a
# runtime image for API 36, so 35 is the highest level it can run tests
# against - ungeprueft, not verified against the Robolectric 4.14.1 release
# notes.
sdk=35
