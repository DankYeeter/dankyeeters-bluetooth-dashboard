# T-039 — AK-17 Beobachtungslauf (ui-ux-designer Nachtrag, developer)

**Ziel:** Bei ABR zeigt die Oberflaeche waehrend eines ausdruecklich
gestarteten Beobachtungslaufs Minimum, Zeitanteil ueber einer waehlbaren
Schwelle und Verweildauer je Stufe — jede Zahl mit ihrem Beobachtungsfenster.

**Abnahmekriterium (GOAL.md, wörtlich):**
- **AK-17 (Eine feste Stufe laesst sich belegen, nicht nur vermuten):** Bei
  adaptiver Bitrate zeigt die Oberflaeche waehrend eines Beobachtungslaufs
  drei Groessen, die heute fehlen: das **Minimum** der gemessenen Bitrate ueber
  das Fenster, den **Zeitanteil oberhalb einer waehlbaren Schwelle**, und die
  **Verweildauer je Stufe**. Damit kann der Nutzer entscheiden, ob eine feste
  Stufe tragfaehig ist, statt es zu vermuten.
  **Ehrlichkeitspflicht (folgt aus AK-3 und AK-8):** Jede dieser Zahlen nennt
  **an Ort und Stelle das Fenster**, ueber das sie gilt — „Minimum 606 kbps
  ueber 24 min beobachtet“, nie „Minimum 606“. Eine Aussage ueber Zeitraeume,
  in denen nicht beobachtet wurde, wird nicht gemacht und nicht nahegelegt.
  **Ausdruecklich nicht erfuellt** ist das Kriterium durch das bestehende
  Event-Log: Der `MeasuredBitrateTracker` meldet bewusst untermeldend (erst
  nach drei Lesungen, nur bei groesserem Sprung), eine kurze Einbrechung
  erzeugt dort keinen Eintrag. Ein leeres Log heisst „nichts Gemeldetes“, nicht
  „nichts Passiertes“.
  **Grenze, bewusst gezogen:** Die Erhebung laeuft **nur bei offener
  Oberflaeche**. AK-4 bleibt unangetastet — es gibt keine
  Hintergrundaufzeichnung und damit keine Aussage ueber unbeobachtete Zeit.

- **AK-4 (Hintergrund kostet nichts):** Ohne offene Monitor-Oberflaeche fuehrt
  die App keine wiederkehrende Arbeit aus, die den Audiopfad beruehrt. Jede
  periodische Arbeit im Prozess ist inventarisiert und begruendet.

**Stand (docs/state.md, 22.09.):** Spec liegt (`UI_SPEC.md` ab Z. 2455,
AK-T039-1..16). Nutzer entschied 03.09.: Lauf von Hand gestartet; einmaliger
Hinweis beim Start, dass Verlassen des Bildschirms den Lauf verwirft. 22.09.:
Zeitbezug bei jeder der drei Zahlen; Schwelle nur aus den pinnbaren Stufen.

## T-039a — Spec-Nachtrag Starthinweis (ui-ux-designer, Modus: Spec)

**Berührt Dateien:** `UI_SPEC.md` (nur Abschnitt T-039) · **Parallel:** ja
(T-041b/c laufen, andere Dateien) · Kein Commit, macht der Director.

Die Spec traegt den einmaligen Starthinweis nicht (Offene Frage 4, Z. ~2911,
verlegt den Satz nur hinter das Fragezeichen). Spezifizieren: Form (bestehende
Muster bevorzugen), verbindlicher englischer String im Abschnitt „Wortlaut“,
Bedeutung von „einmalig“ (kleinste Loesung, die den Nutzerwunsch erfuellt, mit
Begruendung in einem Satz; AK-4 und AK-T002-13 beachten; AK-17 „keine
Persistenz“ meint die Messdaten des Laufs — ob eine DataStore-Einstellung
dafuer vertretbar ist, begruenden). Neues **AK-T039-17** mit pruefbarem Test.
„Offene Fragen“ 1–4 als entschieden markieren (Datum, Antwort), nicht loeschen.

Rueckgabe: Kontraktblock plus hoechstens fuenfzehn Zeilen mit AK-T039-17 wörtlich.

## T-039b — Umsetzung (developer, Stufe: normal) — erst nach T-039a und T-041-Merge

Vorgaben wörtlich unten (vom Director aus `UI_SPEC.md` kopiert, nach T-039a
aktualisiert). Test: `./gradlew test`, JUnit-XML-Zaehlung. Kein Geraet noetig.
Vorlauf: kein Auslöser (keine neue Abhaengigkeit, kein Netz, kein Helfer,
nur Oberflaeche und reine Rechnung).
