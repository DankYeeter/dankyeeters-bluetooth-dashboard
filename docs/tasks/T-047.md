# T-047 — Säule 3: geführter Vorher/Nachher-Prozess (AK-11..14, AK-16), Bau ohne Gerät

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen in
der `CLAUDE.md` des Projekts; Abweichungen: Branch `auto/2026-09-23-saeule3`.

**Ziel:** Säule 3 vollständig im Code, abnahmefähig am Gerät zusammen mit
T-036. Grundlage ist der Schnitt P-1..P-9 aus
`docs/berichte/T-045-architect.md` §2.

**Abnahmekriterium (GOAL.md, wörtlich):** AK-11 bis AK-14 und AK-16,
`GOAL.md:88-130` — jede Rolle liest den Wortlaut dort selbst; die
developer-Abschnitte bekommen ihn kopiert.

**Stand (docs/state.md, 23.09.):** „von AK-8..16 nur AK-10 gebaut (leer),
AK-8/9/15 teilweise, AK-11..14 und AK-16 offen — Saeule 3 fehlt."

## Nutzer- und Directorentscheide (23.09., gelten für alle Abschnitte)

- **F1:** Der Vergleich misst bei gepinnten 990 die `dropouts`-Zählung.
- **F2:** A/B, je Arm 15 min; Drift wird im Messrahmen benannt; hat Arm A null
  Ereignisse, sagt die App, dass keine Wirkung nachweisbar ist.
- **F3:** Die App leitet an und liest den Ist-Wert zurück; kein neues
  Helfer-Kommando, kein WLAN-Schalten, kein Leihen der Scan-Schalter.
- **F4:** AK-16 sagt „strukturell nicht bestimmbar", bis T-037 klärt, ob der
  Pakettyp lesbar wird.
- **F5:** „Device test" wird in T-046 entfernt (läuft parallel); P-8 ersetzt
  nichts mehr, sondern steht allein.
- **F-D1 (Director):** Der Rückweg setzt das Profil-Autoapply aus, bis der
  Nutzer es wieder einschaltet.
- **P-3 (Geräteinventur) fällt in die Gerätesession.** Der Code-Schnitt darf
  nicht auf ihr Ergebnis warten: Lesbarkeit von `wifi_on`,
  `wifi_scan_always_enabled`, `ble_scan_always_enabled` wird zur Laufzeit
  festgestellt; nicht lesbar heisst „nicht lesbar" auf dem Schirm, nichts
  wird abgeleitet.
- **P-1** läuft in T-046.

## T-047a — Entscheidungen eintragen und Bauschnitt (architect)

**Modus:** Entscheidungstiefe voll für AD-030 ff., sonst knapp.

1. Die drei in T-045 „vorgesehenen Entscheidungen" (zwei `ObservationRun`s
   plus reine Funktion statt `ComparisonRun`; Ledger „geliehen"/„gewählt" als
   benannter Widerspruch zu AD-007; Prozess gegen T-005/AD-003/AD-008) sowie
   F1–F4 und F-D1 als AD-030 ff. in `ARCHITECTURE.md` eintragen. AD, die
   `DeviceDiagnostic` nennen, als abgelöst durch T-046 markieren.
2. Bauschnitt für den `developer`: höchstens fünf Dateien mit eigener Logik je
   Lauf, Verdrahtung zählt nicht. Je Lauf: Schritte (P-2, P-4..P-9), Dateien,
   Schnittstellen (Signaturen), Testfälle als Literale (u. a. P-7: 5 gegen 0
   nachweisbar p = 1/32, 4 gegen 0 nicht, α als benannte Konstante
   „Konvention, keine Messung"), Reihenfolge und welche Läufe parallel in
   Worktrees gehen können (disjunkte Dateilisten).
3. Je Lauf angeben, ob er Einstellungen schreibt (Auslöser für den
   Security-Vorlauf), und welche Oberflächentexte die UI-Spec liefern muss.

**Berührt Dateien:** nur `ARCHITECTURE.md`. **Parallel:** ja, T-046
(`developer`) ändert Code, nicht `ARCHITECTURE.md`; Stellen im Code, die T-046
entfernt (`DeviceDiagnostic`, `MonitorScreen.kt:242,414`), nicht als Bestand
voraussetzen. **Scratchpad:** `<Scratchpad>/T-047a/`.

**Scope-Grenzen:** kein Code, keine `UI_SPEC.md` (die schreibt danach der
`ui-ux-designer`), keine neuen Helfer-Kommandos, keine Dependency.

**Prämissen:** Schnitt und Fragen · `docs/berichte/T-045-architect.md` §2–4 ·
23.09. — Nutzerentscheide · `docs/state.md` „Vom Nutzer entschieden (23.09.)" ·
23.09.

**Rückgabe:** Kontraktblock plus höchstens dreissig Zeilen; Bauschnitt in
`ARCHITECTURE.md` (Abschnitt zu AD-030 ff.), nicht im Bericht.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
