# T-046 — Kleinkram vor Säule 3: Device test raus, Pausengrenze, Herkunft, Whitelist-KDoc (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen in
der `CLAUDE.md` des Projekts; Abweichungen für diesen Auftrag: Branch
`auto/2026-09-23-saeule3` (nicht `master`), dort committen, nicht pushen.

**Ziel:** Vier kleine, vom Nutzer am 23.09. entschiedene oder als Befund
beschlossene Änderungen, damit Säule 3 (T-047) auf sauberem Stand beginnt.

**Stufe/Modus:** normal (vier Punkte, sieben Dateien, davon zwei gelöscht)

**Abnahmekriterium (GOAL.md, wörtlich):** AK-12 „Der Prozess enthaelt nur
Belegtes" (GOAL.md:95); AK-8 Herkunft MEASURED/DERIVED; AK-10 „in der
Whitelist benannt".

**Stand (docs/state.md, 23.09.):** „„Device test“ jetzt entfernen, eigener
kleiner Schritt (T-045 F5, AK-12)." · „Beobachtungslauf: Luecke >
`RUN_GAP_MAX` (2 min) beendet den Lauf mit genanntem Grund".

## Kontext

Quittung T-045 (`docs/berichte/T-045-architect.md`): der „Device test"
verletzt AK-12 („Most stable codec") und wird später vom geführten Ablauf
(P-8) ersetzt; P-1 ordnet die AK-8-Wörter; Nebenfund 3 ist der
Whitelist-KDoc.

## Vorgaben

**1 — Device test entfernen (T-045 F5, Nutzer 23.09.: „entfernen, sofort").**
Button „Device test" `MonitorScreen.kt:242`, Berichtstext `MonitorScreen.kt:414`,
Aufrufe in `MonitorViewModel.kt`, die Klasse
`core-monitor/.../diagnostic/DeviceDiagnostic.kt` samt
`DeviceDiagnosticRunnerTest.kt`. Alles, was nur ihm diente, fällt mit
(Strings, Zustände, Imports). Kein Ersatz, kein Platzhalter.

**2 — Pause beendet den Lauf (Nutzer 23.09.).** Spec ist bereits korrigiert
(`UI_SPEC.md` T-039, Tabelle „Unterbrechungen", Zeile „Wiedergabe pausiert"):
„Dauert die Pause laenger als `RUN_GAP_MAX_MS`, endet der Lauf, Grundzeile
"after {RUN_GAP_MAX} of paused playback"". Neuer Grund in `RunEnd`
(`ObservationRun.kt:21-39`), gemessen ab der ersten Abfrage mit
`isPlaying == false` bis zur aktuellen Abfrage; eine spielende Abfrage setzt
zurück. Grundzeile wörtlich:
`"Run ended after {RUN_GAP_MAX} of paused playback. {T} observed."` in
`endLine` (`ObservationRunSection.kt:163-175`). Tests: Pause genau
`RUN_GAP_MAX_MS` → läuft weiter; eine Abfrage darüber → endet mit dem neuen
Grund; Pause unterbrochen von einer spielenden Abfrage → läuft weiter.
Negativer und positiver Fall beide (Lehre QA-018: ein positiver Fall bindet
keine Grenze).

**3 — Herkunft (T-045 P-1).** „Der `Honesty`-KDoc ordnet die drei
AK-8-Wörter zu: MEASURED heisst vom System gemeldet, Dauern nach der App-Uhr
sind DERIVED aus Zeitstempeln. `ObservationRun.observedMs` ist eine Summe und
wird DERIVED." `Honesty` steht in `LinkLiveModels.kt:16`. Spec AK-T039-14 ist
korrigiert. Nur KDoc, kein Verhalten.

**4 — Whitelist-KDoc (T-045 Nebenfund 3).** „Der Whitelist-KDoc zählt
„three", `ALLOWED` hat vier Einträge. Fundstellen:
`PrivilegedProtocol.kt:18,20,79` gegen `:45-62`. Ob `:29` und
`BluetoothDeveloperOptions.kt:329` („three dumpsys/ps commands",
geschichtlich formuliert) auch betroffen sind, entscheidet der Kontext."
Klasse: Suchmuster `three` bzw. `3` in Bezug auf Helfer-Kommandos, Bereich
ganzer Hauptcode plus `ARCHITECTURE.md`; Treffer im Bereich behebt derselbe
Lauf, Suchergebnis in den Bericht (K-2), auch wenn leer.

Vorlauf: kein Auslöser (Entfernen und Textkorrektur, kein neuer Zugriff).

## Testumgebung

Laut `CLAUDE.md`; JDK 21 `C:\Program Files\Eclipse Adoptium\jdk-21.0.12.8-hotspot`.
`gradlew clean test --no-build-cache` am Ende; Referenz 2498/0 (`4507182`) —
die Zahl sinkt um die gelöschten Diagnose-Tests, der Bericht nennt Differenz
und Ursache.

Klon-Volllauf: im Hintergrund starten, sobald der letzte Code-Commit steht;
Ausgabe `<Scratchpad>/T-046/klon.txt`; der Bericht nennt Zahl und Uhrzeit.

## Umfang

**Berührt Dateien:** `MonitorScreen.kt`, `MonitorViewModel.kt`,
`DeviceDiagnostic.kt` (löschen), `DeviceDiagnosticRunnerTest.kt` (löschen),
`ObservationRun.kt`, `ObservationRunTest.kt`, `ObservationRunSection.kt`,
`LinkLiveModels.kt`, `PrivilegedProtocol.kt`, ggf. `BluetoothDeveloperOptions.kt`
· **Parallel:** ja, ein `architect` schreibt nur `ARCHITECTURE.md` · Scratchpad:
`<Scratchpad>/T-046/`

## Scope-Grenzen

Kein Säule-3-Code (Ledger, Vergleich, Katalog). Nicht den Root-Satz
„only a rooted phone can change" (F-011, kommt mit T-047 P-4). Nicht
`RUN_GAP_MAX_MS` im Wert ändern. `ARCHITECTURE.md` nicht anfassen — Stellen,
die `DeviceDiagnostic` dort nennen, als Befund melden.

## Prämissen — jede Bestandsbehauptung mit Quelle

- `DeviceDiagnostic` kommt nur in `MonitorViewModel.kt`, `DeviceDiagnostic.kt`,
  `DeviceDiagnosticRunnerTest.kt` vor · `grep -rln DeviceDiagnostic --include=*.kt` · 23.09.
- Die Lückenregel für ausbleibende Abfragen steht in `ObservationRun.kt:134`
  (`READING_GAP`) · gelesen 23.09.
- Eine Pause endet heute nie den Lauf (`ObservationRun.kt:140-141` gibt bei
  `!isPlaying` nur `lastSeenMs` fort) · gelesen 23.09.

## Rückgabe

Antwort: Kontraktblock plus höchstens dreissig Zeilen. Datei
`docs/berichte/T-046-developer.md` nur bei Befunden oder `STATUS:
teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
