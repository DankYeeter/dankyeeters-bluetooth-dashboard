# T-042 — Audit-Reste und QA-012/QA-013 (developer ×2)

**Ziel:** Die Punkte aus dem Ponytail-Audit vom 22.09., die in T-040 keinem
Unterauftrag gehoerten, sind erledigt. Die zwei offenen P3-Befunde QA-012 und
QA-013 sind behoben. Das Verhalten der App bleibt unveraendert.

**Abnahmekriterium (GOAL.md, wörtlich):** „**AK-5 (Regressionsfrei):** Volle
Testsuite gruen; A16-Bau-Regel eingehalten (neue Features auf der bewiesenen
API-Flaeche, sonst versions-gegated mit ehrlicher Meldung).“

**Stand:** Branch `auto/2026-09-23` (autonomer Lauf). T-041a/b/e sind
gemergt, T-041c laeuft noch. Suite nach T-041b: 2492 Tests, 0 Failures (Bericht
T-041b, 23:52; Director misst nach dem Merge von T-041c nach).

## Vorgaben

- Wie T-040: keine Verhaltensaenderung. Was sich nur mit Verhaltens- oder
  Datenformataenderung entfernen laesst, bleibt stehen und wird gemeldet.
- K-2 fuer jede Text- und Verweisaenderung, Suche und Ergebnis in den Bericht.
- Vorlauf: kein Auslöser.

## Testumgebung

Wie `docs/tasks/T-041.md`: JDK
`JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.12.8-hotspot"`,
`./gradlew clean test`, Zaehlung aus JUnit-XML, dazu
`./gradlew assembleDebugAndroidTest`.
**Schritt 0 im Worktree:** `git reset --hard auto/2026-09-23` (nicht
`master`!), `git log -1` zeigt den Commit, den der Director im Dispatch nennt.
Dann `local.properties` aus dem Hauptbaum kopieren.

## T-042a — Nur von Tests benutzte main-Symbole (developer, Stufe: klein)

**Berührt Dateien:** die fuenf main-Dateien unten und ihre Tests · **Parallel:**
ja, Worktree

Das Audit hat diese Symbole als „nur in Tests benutzt“ gemeldet. Das ist eine
Praemisse: vor dem Loeschen mit Grep ueber main und test aller Module pruefen.
Die Director-Suche am 22.09. um 23:55 (`grep -rlw`) hat diese Dateien gefunden:

- `activeMediaSessions`:
  `core-system/.../attach/PlaybackSessionParser.kt`, Tests
  `PlaybackSessionParserTest.kt` und `PlaybackSessionSweepTest.kt`
- `LARGE_MIN`: `app/.../ui/theme/Contrast.kt`, Test `ContrastTest.kt`
- `isMeasuredBitrate`: `app/.../monitor/LiveTraceModel.kt`, Test
  `MonitorTraceModelTest.kt`
- `preferencePresetId`: `app/.../eq/EqViewModel.kt`, Test
  `PreferencePresetDropdownTest.kt`
- `EXTRAPOLATED_INDICES`: `core-audio/.../eq/EqModels.kt`, Test
  `core-hearing/.../NalRCompensationCalculatorTest.kt`

Tests, die nur ein geloeschtes Symbol pruefen, fallen mit weg. Tests, die
ueber ein solches Symbol echtes Verhalten binden, schreibst du auf den
oeffentlichen Weg um. Wie in T-040b bindest du den umgeschriebenen Test mit
einer Gegenprobe: Regel absichtlich brechen, der Test muss rot werden.

## T-042b — Lazies, .gitignore, QA-012, QA-013 (developer, Stufe: normal)

**Berührt Dateien:** `core-monitor/.../MonitorGraph.kt`,
`core-hearing/.../HearingGraph.kt`, `.gitignore`,
`app/src/test/.../monitor/AcceptanceCriteriaGrepTest.kt`,
`app/src/test/.../monitor/LiveLinkPanelScreenTest.kt` · **Parallel:** ja,
Worktree

1. **Lazies:** Handgebaute `synchronized(lock){ _x ?: X().also{_x=it} }` werden
   zu `val x by lazy { … }`. Das ist in Kotlin standardmaessig synchronisiert.
   Das Muster steht in `MonitorGraph.kt` (laut T-040a etwa 10×) und in
   `HearingGraph.kt` (3× `synchronized(lock)`, Director-Grep 23:55).
   Stellen, die bewusst neu bauen oder zuruecksetzen, bleiben. Nenne sie im
   Bericht. Vorbild ist `SystemGraph.kt` aus T-040a (Commit `936a558`).
2. **`.gitignore`:** `.claude/worktrees/` eintragen. Das Verzeichnis taucht
   heute ungetrackt im Status auf.
3. **QA-012** (Befundtext wörtlich aus `qa/findings.md:153`): „**`AK-T009-31`
   ist vakuum-gruen**: kein einziger `TODO(`-Marker im gescannten Baum, und als
   einzige der sechs Regeln **ohne** Vollstaendigkeits-Guard (die anderen
   nutzen `assertNone`, das `files.isNotEmpty()` erzwingt)“. Ort:
   `AcceptanceCriteriaGrepTest.kt`. Gib der Regel denselben
   Vollstaendigkeits-Guard wie den anderen fuenf. Rot-vorher-Beleg: Mit leerer
   Dateiliste muss der Test rot werden.
4. **QA-013** (wörtlich, `qa/findings.md:154`): „`AK-T009-29` zeigt auf einen
   T-018-Bestandstest mit zwei `assertHides`-Substrings ueber zwei Zustaende —
   **keine ContentDescription, kein Leer-Guard**. Ungleich robust zum
   AK-T002-12-Sweep, der acht Zustaende plus Semantik prueft“. Den Test fuer
   AK-T009-29 auf die Robustheit des AK-T002-12-Sweeps bringen: Zustaende,
   Semantik bzw. ContentDescription, Leer-Guard. Den Text von AK-T009-29 liest
   du in `UI_SPEC.md` (`grep -n AK-T009-29`).

## Scope-Grenzen

- Keine Aenderung an `docs/`, `GOAL.md`, `UI_SPEC.md`, `ARCHITECTURE.md` oder
  an den Registern.
- Die `shrink`-Befunde aus dem Audit sind kein Auftrag.

## Rückgabe

Kontraktblock plus hoechstens dreissig Zeilen, darin die Testzahl mit Uhrzeit
und `git diff --shortstat` gegen den Startcommit. Eine Datei
`docs/berichte/T-042<x>-developer.md` nur bei Befunden oder bei `STATUS:
teilweise/blockiert`.
