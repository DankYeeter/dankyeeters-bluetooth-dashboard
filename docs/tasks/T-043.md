# T-043 — Prüfphase am Quellstand nach T-039..T-042 (qa-engineer, security-reviewer, ui-ux-designer)

**Ziel:** Der Stand nach dem autonomen Lauf ist adversarial geprüft: Die
Bereinigung T-040..T-042 hat nichts gebrochen. AK-17 ist über T-039 erfüllt.
Die bestehenden Features halten ihre Abnahmekriterien, soweit sich das ohne
Gerät prüfen lässt. Alle drei Rollen prüfen **denselben eingefrorenen Stand**,
den der Director im Dispatch nennt. Keine Rolle ändert Code.

**Abnahmekriterien (GOAL.md, wörtlich):**
- „**AK-3 (Ehrlichkeit bleibt):** Kein Wert wird interpoliert, geschaetzt oder
  als MEASURED ausgegeben, wenn er es nicht ist. Degradierte Pfade melden
  "cannot check", nie einen falschen Freispruch.“
- „**AK-4 (Hintergrund kostet nichts):** Ohne offene Monitor-Oberflaeche fuehrt
  die App keine wiederkehrende Arbeit aus, die den Audiopfad beruehrt. Jede
  periodische Arbeit im Prozess ist inventarisiert und begruendet.“
- „**AK-5 (Regressionsfrei):** Volle Testsuite gruen; A16-Bau-Regel eingehalten
  (neue Features auf der bewiesenen API-Flaeche, sonst versions-gegated mit
  ehrlicher Meldung).“
- „**AK-6 (Sicherheit):** Jede Erweiterung des privilegierten Helpers ist vom
  `security-reviewer` geprueft; keine Verbreiterung der Angriffsflaeche ohne
  dokumentierte Begruendung.“
- AK-17: wörtlich in `docs/tasks/T-039.md`, Kopf.

**Stand (eingefroren):** `auto/2026-09-23` @ `efbcf0f` + dieser Auftrag. Director 23.09.
00:38 und 00:39, je `clean test --no-build-cache`: **2493 Tests, 0 Failures** (zweimal).
Aber T-039b sah im selben Befehl 00:30 **2 Failures** in `EqScreenVolumeTiltTest`
(„the feature is off until it is switched on“) und `VolumeAwareTiltViewModelTest`
(„the volume is ignored while the feature is switched off“) — **F-006, Verdacht
Flaky**, einzeln und paketweise gruen. Der
Zyklus beginnt bei `5f62b15` (22.09.). Diff des Zyklus:
`git diff 5f62b15..<Stand>`.

## Kontext

| Paket | Inhalt | Bericht |
|---|---|---|
| T-040a/b | toter Code, Einzel-Interfaces, Stdlib-Tausche, Icons aus `material-icons-extended` als eigene ImageVectors, Altdokumente nach `docs/archiv/` | `docs/berichte/T-040a-developer.md`, `T-040b-developer.md` |
| T-041a | F-001 (androidTest kompiliert), F-002 (Verweise) | Kontraktblock |
| T-041b | MiniJson → org.json (AD-026), Legacy-Literal-Tests | Kontraktblock; P4 F-004/F-005 |
| T-041c | Kalibrierung entfernt, Room 3→4 `DROP TABLE` (AD-028) | Kontraktblock |
| T-041e | Helfer-Token-Vergleich `MessageDigest.isEqual`, Socket-Erbe weg (AD-027, Vorlauf T-041d PASS) | Kontraktblock |
| T-042a/b | test-only-Symbole, `by lazy` in den Graphen, QA-012/QA-013 | Kontraktblock |
| T-039b | AK-17 Beobachtungslauf, AK-T039-1..17 | Kontraktblock |

## T-043a — qa-engineer (opus)

1. **T-039/AK-17:** Prüfe jedes Kriterium AK-T039-1..17 gegen die Spec, die
   wörtlich in `docs/tasks/T-039.md` steht. Das Urteil gilt je Kriterium.
   **Sag ausdrücklich, ob der vorhandene Test das Kriterium prüft oder nur,
   dass der Code tut, was er tut.** Belege das mit einer Rot-vorher-Mutation
   je tragendem Kriterium. Mindestens nötig sind AK-T039-2, -3, -17 und die
   Unterbrechungsgründe.
2. **Regression der Bereinigung:**
   - JSON: Gespeicherte Nutzerdaten müssen lesbar bleiben, auch ausserhalb
     der Legacy-Literale. Suche Eingaben, die MiniJson las und org.json anders
     liest.
   - Migration 3→4: Der Verlauf muss erhalten bleiben.
   - Icons: Gleichheit belegt oder nur behauptet?
   - Graphen mit `by lazy`: Stimmt die Initialisierungsreihenfolge, bleiben
     die Laufzeitnebenwirkungen erhalten (AK-4, keine frühere Arbeit)?
3. **F-006 (Flaky EQ-Tests):** Ursache eingrenzen (geteilter Zustand, Reihenfolge,
   Zeit, Coroutine-Dispatcher?) — Datei:Zeile der Ursache, kein Fix.
4. **Offene Punkte aus T-039b an QA:** Ein beendeter Lauf zeigt "Start", auch
   wenn die Rate gerade nicht lesbar ist; Neustart endet dann sofort mit „rate
   stopped being readable“. Waehrend einer laufenden Pause erscheint die
   Lueckenzeile erst, wenn wieder gemessen wird. Beurteilen gegen die Spec.
5. **QA-012 und QA-013 nachprüfen.** Laut Entwickler behoben; die
   QA-Bestätigung fehlt noch.
6. **Bestehende Features:** eine Stichprobe über die Säulen in `GOAL.md`,
   Sichtbarkeit, Kontrolle und Optimieren, soweit sie sich ohne Gerät prüfen
   lässt. Wähle die drei riskantesten Pfade selbst und begründe die Wahl in
   einem Satz. Liste, was nur mit Gerät prüfbar ist. Diese Liste geht in die
   Gerätesession am 23.09. abends.

Rückgabe: Urteil PASS, CONCERNS oder FAIL. Befunde mit P1–P4, Datei:Zeile und
Reproduktion. Datei `docs/berichte/T-043a-qa-engineer.md`.

## T-043b — security-reviewer

Gegenstand ist der Diff des Zyklus, `5f62b15..<Stand>`, mit diesen
Schwerpunkten:

- Helfer-Protokoll: den Stand nach W-1 gegen die Vorgaben V-1..V-11 prüfen,
  die in `docs/tasks/T-041.md` stehen.
- Datenhaltung:
  - Migration 3→4 löscht Daten, ist das korrekt begrenzt?
  - Das neue Starthinweis-Flag.
  - org.json liest gespeicherte Dateien, dazu F-005 (Zeichen hinter dem Array).
- Entfernte oder geänderte Abhängigkeiten.
- **Offener Punkt aus `docs/state.md`:** `AudioEffectSessionReceiver` ist
  exportiert, das eigene Review steht noch aus. Prüfe das jetzt.

Rückgabe: Befunde SR-### mit Angriffspfad. Datei
`docs/berichte/T-043b-security-reviewer.md` nur bei Befunden.

## T-043c — ui-ux-designer (Review-Modus)

- Den Abschnitt Beobachtungslauf gegen die T-039-Spec prüfen: Zustände,
  Wortlaut, Starthinweis-Dialog und AK-T002-13, also kein Text länger als zwei
  Sätze.
- Die ersetzten Icons prüfen: optisch gleich?
- **Frage des developer (T-039b), entscheiden:** Die Spec widerspricht sich bei
  „Wiedergabe pausiert“ (= Lücke) gegen „Rate nicht mehr lesbar“ (= Lauf
  endet). Umgesetzt: läuft nichts (`isPlaying` false) → Lücke; läuft Wiedergabe
  ohne Rate → Lauf endet. `RUN_GAP_MAX` misst den Abstand zweier Abfragen, egal
  ob mit Rate. Minuten und Prozent werden abgeschnitten (91 s → „1 min“).
  Bestätigen oder korrigieren; bei Korrektur die Spec in `UI_SPEC.md` anpassen.
- Ohne Gerät, am Code und an Robolectric/Compose-Tests. Was nur am Gerät
  prüfbar ist, liste auf.

Rückgabe: Findings in `DESIGN_REVIEW.md` (deine Datei), Antwort mit
Kontraktblock.

## Scope-Grenzen

- Keine Rolle ändert Code, Tests oder `docs/state.md`.
- Befunde P3/P4 und neue Wünsche werden notiert, nicht gefordert.

## Rückgabe (alle)

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```

## Ponytail am Beta-Gate (Director, 23.09. 00:41) — geht in den Fixauftrag

Durchsicht des T-039b-Diffs `9bbacfa..efbcf0f` (Explore). Übernommen werden
nur Befunde der Arten delete, stdlib, native und yagni:
- delete: `@OptIn(ExperimentalLayoutApi::class)` an `ObservationRunSection`
  wirkt nicht, weil nur `RunFigures` `FlowRow` nutzt
  [`ObservationRunSection.kt:37`].
- stdlib: `formatSpan` holt die 60 zweimal über `TimeUnit`. Stattdessen
  `/60` und `%60` oder `kotlin.time`, dann fällt der `TimeUnit`-Import weg
  [`ObservationRunSection.kt:23,182-189`].
- stdlib: `LdacQuality.pinnable.filter { it != LdacQuality.ADAPTIVE }` wird
  zu `LdacQuality.pinnable - LdacQuality.ADAPTIVE`
  [`ObservationRunSection.kt:215`].
- yagni: Fünf Callback-Parameter werden durch `LiveLinkPanel` nur
  durchgereicht. Stattdessen den Controller oder ein
  `(RunAction) -> Unit` übergeben
  [`LiveLinkPanel.kt:93-98,132-140`, `MonitorScreen.kt:109-114`].
- yagni: `RateReading` ist eine öffentliche Datenklasse nur für privaten
  Zustand. Stattdessen private Felder oder `private`
  [`ObservationRun.kt:252-253`].

**Nicht übernommen:**
- „`RUN_MAX_LEVELS` streichen“: Die Grenze ist von der Spec vorgegeben
  (`UI_SPEC.md:2792`) und schützt den Speicher.
- Alle shrink-Befunde.
