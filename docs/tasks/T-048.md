# T-048 — Geräteprüfliste Beobachtungslauf und Update (qa-engineer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen in
der `CLAUDE.md` des Projekts; Abweichungen: Branch `auto/2026-09-23-saeule3`,
Stand ab `6e7fa44` (enthält T-046: Device test entfernt, Pause > 2 min beendet
den Lauf). Parallel arbeiten developer in Worktrees — im Hauptbaum nichts
committen ausser deinem Bericht.

**Ziel:** Die zehn „Nur mit Gerät"-Punkte aus der Prüfphase T-043 am echten
Gerät abhaken, dazu die neue Pausenregel aus T-046.

**Stufe/Modus:** Geräteprüfung am Quellstand (Debug-Build aus dem Quellbaum).

**Abnahmekriterium:** AK-17 und die AK-T039-1..17 aus `UI_SPEC.md` T-039,
soweit nur am Gerät prüfbar; AK-4 (keine periodische Arbeit ohne offenen
Monitor).

**Stand (docs/berichte/T-043a-qa-engineer.md:131, wörtlich):**
1. Lauf mit echten ABR-Lesungen über mindestens 2 min, alle drei Kennzahlen mit Fenster, Kadenzwechsel 1/5 s.
2. Pause und Weiterspielen (`mIsPlaying`); Stopp während der Pause.
3. Bluetooth aus, Kopfhörer aus, Helfer beendet: Lauf endet oder läuft weiter? Enthält der Dump dann einen Geräteblock mit `isConnected=false` oder keinen?
4. Codec-Wechsel LDAC→AAC während des Laufs, danach „Start“ (Befunde A und B).
5. Wechsel HIGH→MID bei gepinntem Lauf (M12); Wechsel der Sample-Rate mitten im Lauf.
6. Starthinweis: Erster Tap, dann „Not now“, dann App beenden (Prozess töten), neu starten, Tap; das Gleiche mit „Continue“.
7. Chipgrösse ≥ 48 dp.
8. Nach dem Update: `monitor.db` v3→v4 auf dem Gerät, Verlauf noch da. Gespeicherte Präferenzprofile lesbar.
9. Icons optisch in der Navigationsleiste und in den Hilfeknöpfen.
10. AK-4: ohne offenen Monitor keine periodische Arbeit (`dumpsys jobscheduler`/`alarm`, CPU im Hintergrund).

**Zusatz 11 (T-046):** Pause länger als 2 min beendet den Lauf mit
„Run ended after 2 min of paused playback. {T} observed."; Pause kürzer läuft weiter.

## Kontext

Die App ist auf dem Telefon **derzeit nicht installiert** (`dumpsys package`
leer, 23.09. 18:00). Punkt 8 (Update v3→v4) ist damit am Gerät nicht mehr
prüfbar, es sei denn, eine ältere Version wird zuerst installiert
(`git tag`: v0.1.0, v0.2.0) — entscheide selbst, ob das mit vertretbarem
Aufwand geht, sonst „nicht prüfbar, weil …".

## Vorgaben

- **Nur ein adb:** `C:\Users\Daniel\Desktop\ClaudeCode\android-sdk\platform-tools\adb.exe`
  (Risiko R-2: zwei adb-Binaries killen sich den Server). Gerät `67011FDKX004XG`.
- Bedienung per `adb shell input`/`screencap`/`uiautomator dump`, Medien per
  `input keyevent` (MEDIA_PLAY_PAUSE). Der Nutzer ist anwesend, aber knapp:
  **Braucht ein Punkt den Nutzer** (Kopfhörer aus/an, Handgriff am Telefon),
  bündle alle solchen Handgriffe, beende deinen Lauf mit
  `STATUS: teilweise`, `BLOCKIERT DURCH: Nutzer — <genaue Handgriffe, Reihenfolge, Dauer>`.
  Der Director gibt sie weiter und setzt dich per Nachricht fort. Höchstens
  **zwei** solcher Unterbrechungen, zusammen höchstens 10 min Nutzerzeit.
- Zuhören wird vom Nutzer nie verlangt; Musik muss nur laufen (Nutzer startet
  sie, falls nötig, in der ersten Unterbrechung).
- PII-Konvention aus `docs/state.md`: MACs `xx:xx:xx:xx:ab:cd`, SSID/IP als Platzhalter.
- Zeitbudget: 40 min. Danach misst ein performance-tuner am selben Gerät — am
  Ende die App **installiert lassen**, Monitor-Ansicht schliessen, keine
  Hintergrundprozesse von dir offen.
- Vorlauf: kein Auslöser.

## Testumgebung

Laut `CLAUDE.md`; JDK 21 `C:\Program Files\Eclipse Adoptium\jdk-21.0.12.8-hotspot`.
Build/Install aus dem Hauptbaum: `gradlew :app:installDebug`.

## Umfang

**Berührt Dateien:** nur `docs/berichte/T-048-qa-engineer.md` · **Parallel:** ja
(developer in Worktrees, kein anderer am Gerät) · **Scratchpad:**
`<Scratchpad>/T-048/`

## Scope-Grenzen

Kein Code-Fix. Keine Säule-3-Prüfung (noch nicht gebaut). Befunde mit Prio
und Adressat in den Bericht, IDs vergibt der Director.

## Prämissen — jede Bestandsbehauptung mit Quelle

- Gerät per adb erreichbar, App nicht installiert, WLAN aus · `adb devices`,
  `dumpsys package dev.dankyeeter.btdashboard`, `settings get global wifi_on` · 23.09. 18:00
- Suite am Stand `6e7fa44`: 2480/0 · Director, `clean test --no-build-cache`, 23.09. 17:57

## Rückgabe

Kontraktblock plus höchstens dreissig Zeilen; Bericht
`docs/berichte/T-048-qa-engineer.md` mit Urteil je Punkt (PASS/FAIL/nicht prüfbar + Beleg).

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
