# T-016 — Retest von T-015 (QA-001 und vier Testbefunde)

Rolle: `qa-engineer`, **Retest** eines gemeldeten Fixes
Angelegt: 2026-09-02 durch den Director
Prueft: `5ccd99c`, `e0dbdb5`, `00d9d26`, `42e1793` auf `master`.

## Ziel

Deine acht Befunde aus T-014 sind adressiert. Pruefe nach, ob sie **wirklich**
weg sind — und ob der Fix nichts kaputt gemacht hat, was vorher hielt.

Erwartung: `./gradlew.bat --console=plain test --rerun-tasks` → **2361 / 0**
(vorher 2344). `--rerun-tasks` ist Pflicht, sonst meldet Gradle nur
`UP-TO-DATE` und liefert keine eigene Evidenz.

    export JAVA_HOME=~/tools/jdk/jdk-21.0.12.1+1
    export ANDROID_HOME=~/tools/android-sdk

## Was der `developer` gemacht hat

- **QA-001:** `underflows` aus `hasLoss`, `hasLossThisWindow`, `lossEvent`,
  `LinkEvent.lossParts()`, `LossRow` und `LiveTraceModel` entfernt.
  `EncoderStarvationTripwire` unberuehrt.
- **Underflow bleibt sichtbar** (AK-2): neue eigene Zeile in ruhiger Farbe,
  Wortlaut "3 encoder underflows in the last 2 s.", sichtbar nur wenn der
  Zaehler sich bewegt hat. Typischer M-5-Anblick: "No loss this window." mit
  der Zaehlerzeile darunter.
- **QA-002:** je ein Fall mit **nur** Dropouts und **nur** Dropped,
  Zusicherungen auf den Kanalnamen statt auf das Boolean.
- **QA-003/-004:** Kommentar bzw. Testtitel zurueckgenommen.
- **QA-006:** Einschraenkung im KDoc benannt (null heisst auch "Wert nicht
  lesbar"), mit Test.

## Der Kern deines Retests

**Nimm ihm die Mutationsproben nicht ab — mach deine eigenen.** Er meldet vier
Proben, die jetzt fangen. Genau diese Art Beleg war in T-014 der Grund, warum
die Luecke ueberhaupt sichtbar wurde. Pruefe:

1. **Faellt wirklich ein Test, wenn man `underflows` ins Verdikt zurueckholt?**
   Auf **allen** Pfaden — Panel, Ereignis, Graph, Datenbank.
2. **Ist der Dropout-Kanal jetzt bewacht?** Streiche `dropouts > 0` aus
   `hasLoss` und aus `LossRow` — faellt es jeweils auf?
3. **Haelt AK-2?** Loesche die neue Underflow-Zeile — merkt es ein Test?

## Zwei Stellen, die er selbst als ungeprueft meldet

- **Offload-Luecke, seine eigene Meldung:** Die neue Underflow-Zeile in
  `LossRow` haengt **nicht** an der Offload-Bedingung. Bei ausgelagert
  kodiertem Link koennte sie erscheinen, obwohl die Zaehler zu einer fremden
  Sitzung gehoeren. Er hat dafuer nichts geprueft und keinen Test geschrieben.
  **Das ist dein wichtigster neuer Pruefpunkt** — es waere ein Verstoss gegen
  AK-3 (keine falsche Behauptung).
- **`MonitorEventLogTest:77`** fuehrt den Satz "Audio was lost in a 2 s window:
  3 app underruns, 1 encoder underflow." als Fixture. Der Test bleibt gruen
  (er rendert nur einen String), aber **dieser Satz kann vom Code nicht mehr
  erzeugt werden.** Bestaetige das oder widerlege es.

## Die Testaenderung, die er gemacht hat — pruefe sie gegen

Er hat **zwei Zahlen** in `MonitorTraceModelTest` geaendert (4 auf 2 und 5 auf
4), weil sie die entfernte Rechnung festschrieben. **Ich habe das bestaetigt**
(Begruendung: `TracePoint.lossCount` traegt "everything audible", und Underflow
ist nachweislich nicht audible). Pruefe, ob er dabei wirklich nur die Zahlen
bewegt hat und nicht das Szenario.

## Weitere Randfaelle, die er nennt

Erster Poll (`txDelta` ist null), Zaehlerruecksetzer beim Stack-Neustart,
Graph-Bildunterschrift.

## Scope-Grenzen

- **Du schreibst nichts.** Befunde als Bericht an mich.
- Kein Geraet — das Telefon ist abgesteckt. Der Geraetebeleg bleibt offen und
  ist **kein** Befund gegen diesen Fix.
- Nicht QA-005, QA-007, QA-008 und AK-T009-29 erneut aufrollen —
  zurueckgestellt bzw. eigener Zyklus.

## Rueckgabeformat

Uebergabe-Kontrakt-Block, dann je Befund QA-001..004/006: **weg, teilweise
weg, oder wieder da**. Dazu deine eigene Testzahl und die Aussage, ob der
Offload-Pfad haelt.
