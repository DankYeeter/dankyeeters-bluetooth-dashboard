# T-041 — Umbauten nach AD-026..AD-028 und Restbefunde aus T-040

**Ziel:** Die Umbauten JSON (AD-026), Kalibrierung und Datenbank (AD-028)
und das Helfer-Protokoll (AD-027) sind gebaut. Die zwei Restbefunde aus T-040
(F-001, F-002) sind behoben. Der Ton-Umbau AD-025 (W-7..W-9) gehört **nicht**
dazu, er läuft in der Gerätesession am 23.09.

**Abnahmekriterium (GOAL.md, wörtlich):** „**AK-5 (Regressionsfrei):** Volle
Testsuite gruen; A16-Bau-Regel eingehalten (neue Features auf der bewiesenen
API-Flaeche, sonst versions-gegated mit ehrlicher Meldung).“ · „**AK-6
(Sicherheit):** Jede Erweiterung des privilegierten Helpers ist vom
`security-reviewer` geprueft; keine Verbreiterung der Angriffsflaeche ohne
dokumentierte Begruendung.“

**Stand (gemessen vom Director, 22.09.2026 23:33, `6c2cde3`):** `gradlew test`
→ 2484 Tests, 0 Failures, 0 Errors (JUnit-XML).

## Kontext

T-040a/b sind gemergt (`5117477`, `6c2cde3`). T-040c hat AD-025..AD-029 in
`ARCHITECTURE.md` festgelegt. Laut AD-029 sind die Stränge disjunkt. Der
Director hat W-2..W-4 zu einem Auftrag zusammengelegt und ebenso W-5..W-6,
weil jeder Strang nur nacheinander geht und ein Lauf ihn am Stück schafft.

## Vorgaben

Die AD-Texte stehen in `ARCHITECTURE.md` bei AD-026, AD-027, AD-028 und
AD-029. **Lies die jeweilige AD vollständig, bevor du anfängst.** Wörtlich
gelten aus der Übergabe des `architect` (22.09.):

- „Keine AutoMigration, kein `org.json:json`, kein kotlinx in `:core-hearing`.“
- „In W-1 weder die AIDL-Schnittstelle noch die Helfer-Version ändern.“
- „Keine JSON-Schlüssel ändern.“
- „`CodecModeSignature`, `LdacModeSignatures` und `readOnce` nicht anfassen,
  sie gehören nicht zur Kalibrierung.“
- AD-026: Der Legacy-Literal-Test (`parse(LEGACY) == fixture`, Literal aus dem
  heutigen MiniJson-Encoder) wird **vor** dem Umbau grün committet und bleibt
  danach unverändert grün.
- AD-028: `MIGRATION_3_4` von Hand geschrieben, mit `DROP TABLE IF EXISTS`.
  Der Migrationstest prüft ausdrücklich, dass die Tabelle danach fehlt.
- Director-Entscheid 22.09.: Die Kalibrierzeilen dürfen gelöscht werden.
  Robolectric als Testabhängigkeit in `:core-hearing` ist freigegeben.
- K-2 für jede Text- und Verweisänderung, Suche und Ergebnis in den Bericht.
- Vorlauf: T-041d, `security-reviewer`, nur für W-1. Die anderen Stränge
  haben keinen Auslöser: keine neue Abhängigkeit im Produkt, kein Netz, kein
  Helfer.

## Testumgebung

Siehe Projekt-`CLAUDE.md`. JDK:
`JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.12.8-hotspot"`.
Befehl: `./gradlew test --console=plain`, Zählung aus den JUnit-XML-Dateien.
Dazu `./gradlew assembleDebugAndroidTest`: nach T-041a muss das grün sein.

**Schritt 0 im Worktree:** `git reset --hard master`, `git log -1` zeigt den
Director-Commit, der T-041 anlegt. Dann `local.properties` aus dem Hauptbaum
`C:\Users\Daniel\Desktop\ClaudeCode\dankyeeters-bluetooth-dashboard\` kopieren.

Klon-Volllauf: In T-040 scheiterte er zweimal (Pfadlänge,
Isolations-Hook). Die Suite im sauberen Worktree mit `clean test` genügt.
Nenne Zahl und Uhrzeit.

---

## T-041a — F-001 und F-002 (developer, Stufe: klein)

**Berührt Dateien:**
`app/src/androidTest/java/dev/dankyeeter/btdashboard/privileged/adb/AdbReachabilityTest.kt`
sowie reine Kommentar- und Verweiszeilen in den 14 Dateien unten.
**Parallel:** ja, im Worktree · **Scratchpad:** `…/scratchpad/T-041a/`

- **F-001:** `AdbReachabilityTest.kt:35,38,58` ruft `AdbPortDiscovery.find(...)`
  auf, es gibt aber nur `findAll`. Stelle den Test auf die heutige API um,
  **ohne die Absicht des Tests zu ändern**. Danach muss
  `assembleDebugAndroidTest` grün sein.
- **F-002:** Verweise auf die acht nach `docs/archiv/` verschobenen Dokumente
  umstellen. Director-Grep 22.09. 23:40 (Regex: Dateiname ohne vorangehendes
  `/`) findet 29 Zeilen in 14 Dateien: `Audiogram.kt`,
  `ClinicalAudiogram.kt`, `Compensation.kt`, `FitCheck.kt`,
  `MedianAudiogramAggregator.kt`, `NalR.kt`, `NalRCompensationCalculator.kt`,
  `NalRCompensationCalculatorTest.kt`, `NalRTest.kt`, `DeviceDiagnostic.kt`,
  `BluetoothBroadcastSource.kt`, `QualityReportSource.kt`,
  `LinkSampleCollector.kt`, `SamplingPolicy.kt`. `T-040a-developer.md` nennt
  33 Zeilen in 17 Dateien. Suche selbst und nenne deine Zahl.

## T-041b — JSON-Strang W-2..W-4 (developer, Stufe: normal)

**Berührt Dateien:** Liste aus AD-029, Zeilen W-2, W-3 und W-4.
**Parallel:** ja, im Worktree · **Scratchpad:** `…/scratchpad/T-041b/`

Reihenfolge und je ein Commit: W-2 (Legacy-Literal, grün auf MiniJson) → W-3
→ W-4 (MiniJson löschen).

## T-041c — Kalibrier-Strang W-5..W-6 (developer, Stufe: normal)

**Berührt Dateien:** Liste aus AD-029, Zeilen W-5 und W-6, einschliesslich des
erzeugten `schemas/.../4.json`.
**Parallel:** ja, im Worktree · **Scratchpad:** `…/scratchpad/T-041c/`

Reihenfolge und je ein Commit: W-5 → W-6. W-6 löscht Nutzerdaten, deshalb:
Der Migrationstest läuft vor der Übergabe gegen eine Datenbank, die aus
`schemas/3.json` mit einer Kalibrierzeile gefüllt ist.

## T-041d — Vorlauf für W-1 (security-reviewer)

**Berührt Dateien:** keine, nur Lesen · **Parallel:** ja

Prüfe AD-027 und den Plan zu W-1 vor der Umsetzung:

- `tokensMatch` in `PrivilegedProtocol.kt:327` (Zeilenstand vor T-040, selbst
  nachsehen) soll auf `MessageDigest.isEqual` umgestellt werden. Die Prüfung
  auf null/leer bleibt.
- `encodeAuth`, `decodeAuth`, `encodeRun` und `decodeRun` sollen gelöscht
  werden, Aufrufer gibt es nur in Tests.
- Die KDoc-Korrektur an `:111-114` gehört dazu.

Fragen:
1. Ist `MessageDigest.isEqual` ab API 31, dem minSdk, konstantzeitig? Laut
   Architekt ist das nur am AOSP-Branch main geprüft.
2. Verbreitert oder verengt W-1 die Angriffsfläche?
3. Gibt es Auflagen für den `developer`?

Rückgabe als Vorgabenblock, den der Director wörtlich in T-041e übernimmt.

## T-041e — W-1 Helfer-Protokoll (developer, Stufe: normal) — erst nach T-041d

Dateien und Test: AD-029, Zeile W-1. Urteil T-041d: **PASS**, keine
Befunde. Vorgabenblock des `security-reviewer` (22.09., Zeilenstand
`00d6b5c`), wörtlich:

- **V-1** `PrivilegedProtocol.kt:328`: Die Prüfung bleibt wörtlich `isNullOrBlank`. Nicht `isNullOrEmpty`, auch wenn AD-027 von „null/leer“ spricht. Randbedingung: `tokensMatch(" ", " ")` wäre nach der Umstellung auf `isEqual` `true`. Heute ist kein erwartetes Token leer oder nur aus Leerzeichen (`PrivilegedServer.kt:128` lehnt das beim Start ab, die gespeicherten Werte sind UUIDs). Die Prüfung ist Tiefenverteidigung und bleibt.
- **V-2** `PrivilegedProtocolTest.kt`, neuer Test im Abschnitt zur Token-Prüfung: `assertFalse(PrivilegedProtocol.tokensMatch(" ", " "))`. Er schlägt fehl, wenn V-1 zu `isNullOrEmpty` wird. `PrivilegedTokenTest.kt:147-149` prüft nur `null` und `""`.
- **V-3** `PrivilegedProtocol.kt:329-334` wird zu `MessageDigest.isEqual(offered.toByteArray(Charsets.UTF_8), expected.toByteArray(Charsets.UTF_8))`, dazu der Import `java.security.MessageDigest` bei `:3`. Reihenfolge: zuerst `offered`, dann `expected`. In der main-Fassung hängt die Laufzeit von der Länge des ersten Arguments ab, und diese Länge soll der Anrufer kennen, nicht die gespeicherte.
- **V-4** `PrivilegedProtocol.kt:316-322`: Die KDoc behauptet heute fälschlich „Length-independent“. Neuer Wortlaut: Der Inhalt wird ohne vorzeitigen Abbruch verglichen. Ob die Länge vorzeitig abbricht, hängt von der Version des ART-Moduls ab (android12-release ja, main nein). Das ist harmlos, weil das Token eine UUID fester, öffentlicher Länge ist und beide Aufrufer vorher die uid prüfen. „the timing leak this closes“ (`:320`) streichen. Suche nach `length.independent|constant.time|time.constant` (ohne Groß-/Kleinschreibung) über den Baum ausser `docs/archiv/` vorher und nachher, Zahl berichten.
- **V-5** `PrivilegedProtocol.kt:165-180` löschen. `stripLineEnding` (`:163`) und `SEPARATOR` (`:153`) bleiben, die übrigen Decoder brauchen sie.
- **V-6** `PrivilegedProtocol.kt:146-151`: „One request per line“ stimmt nach V-5 nicht mehr, Anfragen sind AIDL-typisiert. Den Kommentar auf Antworten beschränken.
- **V-7** `PrivilegedProtocol.kt:447-451` (KDoc von `decodeOrNull`): Den Satz über den Socket ersetzen. Neue Begründung: Die Decoder laufen in der App auf Antworten eines Shell-uid-Prozesses, der erst nach Prüfung von uid und Token angenommen wurde. Das ist weiterhin eine Prozessgrenze, deshalb bleibt `null` statt Exception. `runCatching` (`:452-454`) bleibt unverändert.
- **V-8** `PrivilegedProtocol.kt:111-114`: Die KDoc wandert vor `SHUTDOWN` (`:125`). Werte, `mutates`-Flags und Reihenfolge der Einträge bleiben unverändert.
- **V-9** `PrivilegedProtocolTest.kt`: Tests `:79-83`, `:85-89` und den ganzen Test `:134-139` löschen. Im Test `:121-132` nur `:126-129` löschen; `:130-131` und der Test bleiben, Kommentar `:123-125` bekommt die Begründung aus V-7. `:196` und `:251` sind Einzelzeilen: löschen, der Rest der Tests bleibt. Kommentar `:420-422` wie V-7. `:13` bleibt.
- **V-10** K-2-Suche mit zwei Masken, Trefferzahl vorher und nachher: Maske 1 `git grep -n -i socket` auf `privileged/` in main und test; Maske 2 `git grep -n -E '"AUTH|"RUN|AUTH |RUN |request per line|may connect|may reach'`. Nicht anfassen: Treffer in `privileged/adb/` (echte TCP-Sockets bzw. ADB-`A_AUTH` in `AdbMessage.kt:76,131`), `PrivilegedServer.kt:34-46`, `PrivilegedProvider.kt:236`. Selbst beurteilen: `PrivilegedProtocol.kt:18` („A general shell behind a socket“, Vergleich mit Shizuku).
- **V-11** Nicht anfassen: `PrivilegedServer.kt`, `PrivilegedClient.kt`, `PrivilegedProvider.kt`. Ihre Aufrufe von `tokensMatch` bleiben in der Signatur gleich.

---

## Scope-Grenzen

- Keine Arbeit an W-7..W-9 (Ton, Oboe, NDK, `core-audio/build.gradle.kts`).
- Keine Dateien anderer Unteraufträge anfassen. Wird dort eine Änderung
  nötig, melden.
- Keine Änderung an `GOAL.md`, `UI_SPEC.md`, `ARCHITECTURE.md`,
  `docs/state.md` oder an den Registern.

## Prämissen

- Die Stränge sind disjunkt · AD-029 („Die vier Straenge beruehren keine
  gemeinsame Datei“) · Director hat die Dateilisten am 22.09. gegen die
  F-002-Liste geprüft: keine Überschneidung.
- Kalibrier-Symbole stehen nur in `MonitorGraph.kt` · Director-Grep 22.09.
  22:15 · vor W-5 selbst erneut prüfen.
- DB-Version 3 · `MonitorDatabase.kt:244` · 22.09.

## Rückgabe

Antwort: Kontraktblock plus höchstens dreissig Zeilen, darin die Testzahl aus
JUnit-XML mit Uhrzeit und `git diff --shortstat master`. Eine Datei
`docs/berichte/T-041<x>-<rolle>.md` nur bei Befunden oder bei `STATUS:
teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
