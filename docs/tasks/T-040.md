# T-040 — Codebasis nach Ponytail-Audit bereinigen (developer ×2, architect)

**Ziel:** Toter Code, Einzel-Interfaces, handgebaute Stdlib-Stellen und
ungenutzte Abhängigkeiten sind entfernt, ohne Verhaltensänderung. Die vier
Umbauten mit Verhaltens-, Daten- oder Helfer-Bezug sind vom `architect`
entschieden, bevor jemand sie baut.

**Abnahmekriterium (GOAL.md, wörtlich):** „**AK-5 (Regressionsfrei):** Volle
Testsuite gruen; A16-Bau-Regel eingehalten (neue Features auf der bewiesenen
API-Flaeche, sonst versions-gegated mit ehrlicher Meldung).“ · „**AK-6
(Sicherheit):** Jede Erweiterung des privilegierten Helpers ist vom
`security-reviewer` geprueft; keine Verbreiterung der Angriffsflaeche ohne
dokumentierte Begruendung.“

**Stand (gemessen vom Director, 22.09.2026 22:00, `HEAD 5f62b15`):**
`gradlew test` → 2488 Tests, 0 Failures, 0 Errors, 0 Skipped (gezählt aus
`**/build/test-results/**/TEST-*.xml`).

## Kontext

Der Director hat am 22.09. `/ponytail-audit` über das ganze Repo gefahren
(Explore-Lauf, Ergebnis unten als Prämissen). Der Nutzer hat den Umfang
„auch Umbauten“ freigegeben und die acht Altdokumente im Hauptordner nach
`docs/archiv/` verschoben haben wollen. `ponytail:`-Marker gibt es im Code
keine (Grep 22.09., leer).

## Vorgaben

- Keine Verhaltensänderung in T-040a/b. Was sich nur mit Verhaltens-,
  Datenformat- oder Helferänderung entfernen lässt, gehört nicht zu a/b —
  melden.
- Interfaces, die Test-Fakes tragen, bleiben (Liste unter Scope-Grenzen).
- Kommentare: Code und Kommentare Englisch (Projekt-`CLAUDE.md`).
- K-2 gilt: jeder geänderte Verweis/Text erst nach projektweiter Suche fertig;
  Suchbefehl und Ergebnis in den Bericht.
- Vorlauf (security/compliance) für a/b: kein Auslöser — nur Löschungen und
  Stdlib-Tausche, keine neue Abhängigkeit, kein Helfer-Kommando. Ausnahme
  `MessageDigest.isEqual` in `PrivilegedProtocol.kt` — gehört zu T-040c, nicht a.

## Testumgebung

Siehe Projekt-`CLAUDE.md`. Abweichend vom Stand in `docs/state.md`: JDK liegt
auf diesem Rechner unter
`JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.12.8-hotspot"`.
Befehl: `./gradlew test --console=plain`, Zählung aus den JUnit-XML-Dateien.
androidTest wird nicht ausgeführt (Gerät nicht Teil dieses Auftrags), muss
aber kompilieren: `./gradlew assembleDebugAndroidTest`.

**Schritt 0 für a und b (Worktree):** der Worktree startet auf `origin/HEAD`.
Zuerst `git reset --hard master` (lokaler Branch des Hauptbaums), prüfen, dass
`git log -1` den Director-Commit von heute zeigt, dann
`local.properties` aus dem Hauptbaum
`C:\Users\Daniel\Desktop\ClaudeCode\dankyeeters-bluetooth-dashboard\` kopieren
(ungetrackt, sonst fehlt `sdk.dir`).

Klon-Volllauf: im Hintergrund starten, sobald der letzte Code-Commit steht;
Ausgabe `<Scratchpad>/T-040x/klon.txt`; der Bericht nennt Zahl und Uhrzeit.

---

## T-040a — app, core-system, Altdokumente (developer, Stufe: normal)

**Berührt Dateien:** `app/**` ausser `app/**/privileged/PrivilegedProtocol.kt`
und `IPrivilegedService.aidl` · `core-system/**` · die acht Altdokumente ·
`docs/scan/T-005-ENTWURF.md` (nur Verweiszeile) · `THIRD_PARTY_LICENSES.md`,
`PLAYSTORE_COMPLIANCE.md` (nur Verweiszeilen) · **Parallel:** ja, mit T-040b
(Worktree) und T-040c · **Scratchpad:** `…/scratchpad/T-040a/`

1. `git mv` nach `docs/archiv/`: `ANDROID17_READINESS.md`, `COMPENSATION.md`,
   `HANDOVER.md`, `PLAN-A16-ABNAHME.md`, `PLAN.md`, `PLAYSTORE_COMPLIANCE.md`,
   `REPORT-2026-08-26.md`, `RESEARCH_COMPENSATION.md`. Verweise in Code-
   Kommentaren, `robolectric.properties`, Drawables und den genannten Docs auf
   `docs/archiv/<name>` umstellen. **Nicht** anfassen: `ARCHITECTURE.md`
   (macht T-040c), `docs/state.md` (macht der Director).
2. Löschungen und Tausche aus den Prämissen, soweit sie in `app/**` oder
   `core-system/**` liegen und die Prüfung bestehen.
3. `material-icons-extended` entfernen: die tatsächlich benutzten Icons als
   Vector-Drawables bzw. `ImageVector` ins Projekt übernehmen. Lizenz der
   Icons (Apache 2.0) in `THIRD_PARTY_LICENSES.md` stehen lassen/anpassen.
4. Ungenutzte Gradle-Einträge in `app/build.gradle.kts` entfernen
   (`ui-tooling-preview`, debug `ui-tooling`, `lifecycle-runtime-ktx`,
   androidTest `rules`/`test-core`) — jeweils nur, wenn Build und Tests grün.

## T-040b — core-hearing, core-monitor, core-audio-Probes (developer, Stufe: normal)

**Berührt Dateien:** `core-hearing/**` ausser `hearing/store/MiniJson.kt`,
`DerivedCalibrationJson.kt`, `PreferenceProfileJson.kt` und deren Tests ·
`core-monitor/**` ausser `MonitorGraph.kt`, `data/MonitorDatabase.kt`,
`link/live/CodecModeCalibration.kt`, `data/RoomCodecModeSignatureStore.kt` und
deren Tests · `core-audio/src/androidTest/**` · `core-monitor/build.gradle.kts`
· **Parallel:** ja, mit T-040a (Worktree) und T-040c · **Scratchpad:**
`…/scratchpad/T-040b/`

1. Die sechs assert-losen Probe-Tests in `core-audio/src/androidTest/.../eq/`
   löschen (`AcousticEqTest.kt` bleibt). Tragen ihre KDocs Befunde, die
   nirgends sonst stehen: den Kern je Probe in drei Zeilen in den Bericht.
2. Löschungen, Einzel-Interfaces und Tausche aus den Prämissen, soweit sie in
   deinen Dateien liegen und die Prüfung bestehen.
3. `core-monitor`: `androidx.core.ktx` durch `Context.checkSelfPermission`
   ersetzen (minSdk prüfen) und Dependency entfernen.

## T-040c — Entwurf der vier Umbauten (architect, Entscheidungstiefe: knapp)

**Berührt Dateien:** `ARCHITECTURE.md` · **Parallel:** ja · **Scratchpad:**
`…/scratchpad/T-040c/`

Je Umbau eine AD mit Entscheidung, Alternative, Risiko, Prüfweg (Test oder
Gerät) und Dateiliste für den späteren `developer`-Auftrag:

1. **Tonerzeugung:** Oboe + NDK/CMake/JNI (`core-audio/src/main/cpp/*`,
   `audio/tone/NativeToneGenerator.kt`) durch `android.media.AudioTrack`
   (PCM_FLOAT, MODE_STREAM) ersetzen? Was verlangt der Hörtest an Latenz,
   Pegeltreue und Rampe, und wie wird das am Gerät belegt?
2. **JSON:** `MiniJson` + zwei Codecs durch kotlinx.serialization oder
   Plattform-`org.json` (mit `testImplementation("org.json:json")`) ersetzen —
   **gespeicherte Nutzerdaten müssen unverändert lesbar bleiben**; nenne den
   Test, der das bindet.
3. **Helfer-Protokoll:** AIDL-Methoden liefern Strings mit handcodiertem
   Base64/„OK/ERR“-Format (`PrivilegedProtocol.kt:153-455`,
   `IPrivilegedService.aidl`). Umstieg auf `Bundle`/Parcelable ja/nein;
   Angriffsfläche; dazu `encodeAuth/decodeAuth/encodeRun/decodeRun` (nur Tests)
   und Konstantzeit-Vergleich → `MessageDigest.isEqual` (`:327`).
4. **Kalibrierung + Datenbank:** das Codec-Mode-Kalibrier-Subsystem ist laut
   Audit unerreichbar (Prämisse unten). Entfernen samt Room-Tabelle per
   `@DeleteTable`-AutoMigration 3→4? Welcher Migrationstest?

Ausserdem: den Verweis auf `HANDOVER.md` in `ARCHITECTURE.md` auf
`docs/archiv/HANDOVER.md` umstellen. Keinen Anwendungscode ändern.

---

## Scope-Grenzen

- Nicht entfernen (tragen Test-Fakes): AbsoluteVolumeController,
  CompensationApplier, SecureSettingsController, MediaVolumeController,
  DeviceProfileSource, MediaVolumeSource, SystemEqualizer,
  SystemEqualizerFactory, DpEffect, ToneGenerator, AmbientNoiseCheck,
  InstalledAppSource, PlayingAppsSource, MonitorEventSource,
  PreferenceTestActions, SetupEnvironment.
- `shrink`-Befunde sind kein Auftrag, ausser sie fallen beim Löschen ohnehin an.
- Dateien der anderen Unteraufträge nicht anfassen; nötige Änderung dort melden.
- Keine Änderung an `GOAL.md`, `UI_SPEC.md`, `docs/state.md`, Registern.

## Prämissen — aus dem Audit 22.09. (Explore), je Punkt vor dem Löschen prüfen

Angaben aus Symbolsuchen sind **Prüfaufträge, keine Anordnung** (L-033). Grep
über `main` **und** `test` aller Module; Reflection/Manifest/XML mitdenken.
Hält eine Prämisse nicht: stehen lassen, im Bericht nennen.

Director-Stichprobe 22.09. 22:15 (`grep -rw … --include=*.kt`, ohne `build/`):
Kalibrier-Symbole nur in `MonitorGraph.kt`; sechs Probe-Dateien je 0 `assert`;
`restoreReference`, `GoldRule`, `isReadable`, `unskip` je 1 Treffer;
`stopScan` 4, `hasScanPermission` 3 Treffer (Audit sagt 0 Aufrufer im
DashboardViewModel — **gleichnamige Symbole möglich, genau prüfen**).

**app / core-system (T-040a):**
- delete: Doppelte AirPods→Preset-Vorschlagslogik in `DashboardViewModel.kt:37,62-77` (BluetoothDashboardViewModel:97 macht es schon); `stopScan`, `hasScanPermission`, `DetectedDeviceRepository.clear` (`ui/DetectedDeviceRepository.kt:25`) ohne Aufrufer.
- delete: `GoldRule` (`theme/GoldComponents.kt:71`), `Contrast.isReadable` (`theme/Contrast.kt:46`), `SetupWizardViewModel.unskip` (`:69`), `AdbPairingPacket.TYPE_DEVICE_GUID` (`:108`), `hasUnmetRequirements` (`SetupSteps.kt:195`), `AirPodsBeacon.bothInEar/hasBudBattery` (`:33-36`), `offeredSampleRatesHz/BitsPerSample` (`PrivilegedCodec.kt:185`) — main-Symbole nur von Tests oder gar nicht benutzt.
- stdlib: ~26 handgebaute `synchronized(lock){ _x ?: X().also{_x=it} }` → `by lazy` in `core-system/.../SystemGraph.kt:53-80+`; bewusst neu bauende bleiben.
- stdlib: Längenpacken in `adb/AdbPairingPacket.kt:36-39,74-77` → `DataInputStream.readInt()`/`DataOutputStream.writeInt`.
- yagni: `SecureSettingsState {GRANTED, NOT_GRANTED}` → Boolean (`core-system/.../secure/SecureSettingsState.kt`) — nur wenn nicht persistiert.
- yagni: `SystemGraph.systemProperties` null + `?: NoSystemPropertyReader` → Default-Wert (`:278-286`).

**core-hearing / core-monitor (T-040b):**
- yagni: `HearingTestController` (`HearingTest.kt:19`), `CompensationCalculator` (`Compensation.kt:112`), `AudiogramAggregator` (`Audiogram.kt:104`, `HearingGraph.kt:51`), `ConnectionTicks` (`BtConnectionEvents.kt:27`), `ProcessResolver` (`ForeignEqDetector.kt:91`) — je eine Implementierung, kein Fake.
- delete: `UnavailableCodecStatusSource` (`A2dpCodecStatusSource.kt:227`), `UnavailableDumpsysLinkSource` (`DumpsysLinkSource.kt:31`), `NoAmbientNoiseCheck` (`MicAmbientNoiseCheck.kt:108`) — nie konstruiert.
- delete/move: `UnavailableQualityReportSource` (`QualityReportSource.kt:40`) nur in 7 Testdateien → in Test-`Fakes.kt`.
- delete: `PreferenceProtocol.consistencyThreshold`, `neutralDb` (`PreferenceEngine.kt:123-125`); `VolumeGuard.restoreReference`, `isIntact` (`:82,112`); `AudiogramStore.derivedCalibrationFor`, `currentSelectedRuns` (`:149,214`); `PreferencePool.trialCount` (`:41`); `ForeignEqScanResult.hasForeignEq` (`:19`); `TakeoverAnnotator.currentActiveAddress` (`:115`).
- delete (nur Tests benutzen es): `PresetMatching.presetFor` (`:50`), `CalibrationPreset.provenanceLine`, `requiresFitCheck` (`Compensation.kt:166-176`), `byIdOrGeneric` (`CalibrationPresets.kt:145`), `supportedSampleRates` (`CodecDecoding.kt:175`), `activeMediaSessions` (`PlaybackSessionParser.kt:71`), sowie `hasCachedPass`, `isEncodedOnHost`, `lastReportedKbps`, `consecutiveOverThreshold`, `currentEstimate`, `isMeasuredBitrate`, `preferencePresetId`, `LARGE_MIN`, `EXTRAPOLATED_INDICES` (Ort selbst suchen). Tests, die nur diese Symbole prüfen, fallen mit.
- yagni: `PresentationTiming` nie gesetzt → private Konstanten (`HughsonWestlakeTestController.kt:237`).
- yagni: `AncMode` in main nie ausser UNKNOWN — **nicht** entfernen, wenn es in Backup/Store persistiert wird (Datenformat); dann nur melden.
- shrink, fällt beim Löschen an: `ClinicalAudiogram.medianOf` (`:267`) → `MedianAudiogramAggregator.median`.

**T-040c (nur zur Entscheidung):** Kalibrier-Symbole `codecModeCalibrator`,
`codecModeSignatures`, `installCodecModePinner` nur in `MonitorGraph.kt`
(Director-Grep 22.09.); DB `version = 3` (`MonitorDatabase.kt:244`).

## Rückgabe

Antwort: Kontraktblock plus höchstens dreissig Zeilen: entfernte Zeilen
(`git diff --stat master`), Testzahl aus JUnit-XML mit Uhrzeit, nicht
gehaltene Prämissen. Datei `docs/berichte/T-040<x>-<rolle>.md` nur bei
Befunden oder `STATUS: teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
