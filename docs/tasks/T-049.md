# T-049 — Gerätesession 23.09.: Inventur P-3, Trennmessung T-036, Callback-Probe T-037 (performance-tuner)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen in
der `CLAUDE.md` des Projekts; Abweichungen: Branch `auto/2026-09-23-saeule3`;
parallel arbeiten developer in Worktrees — im Hauptbaum nur deine Dateien
unter `docs/perf/` und den Bericht committen, mit Pfaden hinter `--`.

Drei Abschnitte, **nacheinander, je ein eigener Lauf**. Der Director startet
jeden Abschnitt einzeln.

**Änderung 23.09. 19:19 (Nutzer): kein Nutzer verfügbar, auch kein Zuhören.
Geht vor alle Stellen unten, die Handgriffe des Nutzers vorsehen.**
- T-049a unverändert.
- T-049b: WLAN schaltest du selbst per `adb shell svc wifi enable|disable`.
  Vor dem Messarm „mit" belegst du am Read-back, dass das Telefon einem
  **2,4-GHz**-Netz zugeordnet ist (Frequenz 2412–2484 MHz aus `cmd wifi
  status`/`dumpsys wifi`, PII-Platzhalter). Verbindet es sich nur mit 5/6 GHz
  oder gar nicht: Arm „mit" nicht messen, `STATUS: teilweise` mit Grund —
  keine Ersatzmessung. Musik läuft über Tidal (vom QA-Lauf T-048 gestartet);
  läuft sie nicht, per `input keyevent MEDIA_PLAY` starten und am Dump
  belegen, dass A2DP spielt. Telefon und Kopfhörer werden nicht bewegt.
  Am Ende WLAN in den Zustand von vor der Messung zurück (Vorwert im Bericht).
- T-049c: nur passiv — Callback registrieren und auf ein **natürliches**
  BQR-Ereignis warten, höchstens 20 min; kommt keins: `teilweise`, „kein
  Ereignis ohne Störung", keine künstliche Störung.

## Gemeinsame Vorgaben (alle Abschnitte)

- **Nur ein adb:** `C:\Users\Daniel\Desktop\ClaudeCode\android-sdk\platform-tools\adb.exe`
  (R-2). Gerät `67011FDKX004XG` (Pixel 11 Pro, Android 17), Sink Noble FoKus
  Prestige Encore.
- **Kein `dumpsys bluetooth_manager` zur Zwischenkontrolle — das leert die
  BQR-Queue (R-011).** Die **Monitor-Ansicht der App bleibt geschlossen**: sie
  pollt `dumpsys bluetooth_manager` alle 0,5–5 s (`DumpsysLinkSource.kt:19`,
  T-045 Nebenfund 1). App-Prozess vor jedem Messabschnitt beenden
  (`am force-stop dev.dankyeeter.btdashboard`).
- **Nutzer:** anwesend, aber knapp; Zuhören wird nie verlangt, Musik muss nur
  laufen. Braucht ein Schritt den Nutzer, beende den Lauf mit
  `STATUS: teilweise` und `BLOCKIERT DURCH: Nutzer — <Handgriffe, Reihenfolge,
  Uhrzeit, Dauer>`; der Director gibt es weiter und setzt dich per Nachricht
  fort. Handgriffe so bündeln, dass der Nutzer nicht am Telefon warten muss.
- PII-Konvention aus `docs/state.md` (`SSID_A`, `AP_BSSID`, `IP_1`/`IP_2`,
  MACs `xx:xx:xx:xx:ab:cd`).
- **Read-back deckt das vollständige Zustandsbuch ab, je Abschnitt**, nicht
  nur am Anfang (Verfahrensregel `docs/state.md`, Anlass T-008/T-027).
- Vorlauf: kein Auslöser (Messung, kein Code).

## T-049a — Einstellungs-Inventur P-3 (≈15 min, Nutzer: nichts)

**Ziel (T-045 P-3, wörtlich):** „Geräteinventur am Pixel 11 Pro
(`performance-tuner`, `docs/perf/`). Für jede Audiopfad-Einstellung wird
festgestellt, ob die App sie selbst stellt, per Deep-Link hinführt oder nur
anleiten kann. Dazu gehört die Frage, ob `wifi_on`,
`wifi_scan_always_enabled` und `ble_scan_always_enabled` aus der App-Uid
lesbar sind und mit `WRITE_SECURE_SETTINGS` schreibbar. Ohne
Zwischen-`dumpsys bluetooth_manager` (R-011)." Dazu **F-011**: an welcher
Stelle des Telefons stellt der Nutzer die drei Nur-Lesen-Werte, die die App
heute mit „only a rooted phone can change" beschreibt
(`DeviceProfilesScreen.kt:316`, `BluetoothSystemControls.kt:105`)?
Nur lesen, nichts schreiben ausser zur Probe mit sofortigem Zurücksetzen auf
den gelesenen Wert (Vorwert und Rückstellung im Bericht).
**Ablage:** `docs/perf/T-049a-inventur.md`.

## T-049b — Trennmessung T-036 (≈70 min, Nutzer: ~5 min)

**Ziel (docs/state.md, wörtlich):** „30 min bei gepinnten 990 **mit**
2,4-GHz-WLAN-Assoziation gegen 30 min **ohne**. Sie leistet drei Dinge auf
einmal: 1. Klaert den Widerspruch T-029 gegen T-032. 2. Testet Massnahme 1
aus R-010 — die einzige mit eigener Messung. 3. Liefert die Doppelaufnahme,
die AK-T009-24 am Geraetedump belegbar macht."

**Widerspruch:** T-029 (02.09.) 990 gepinnt, WLAN nie geprüft → 10 Cluster in
25 min; T-032 (03.09.) 990 gepinnt, WLAN nicht assoziiert → 0 Verluste in
27,78 min (`docs/archiv/state-messbefunde-2026-09-03.md:58-66`).

**Bedingungen (docs/state.md, wörtlich):**
- „Fuer die Doppelaufnahme: zwei Dumps im Abstand von ~4 min aus **einem
  Intervall, in dem `underflow` sich nicht bewegt, waehrend `dropouts`
  zaehlen**. Grund: `UI_SPEC.md:2361` formuliert AK-T009-24 als Snapshot ueber
  ein **Fenster** (`underflows` = 0, `dropouts` = 21 **in 97 s**), also
  Fensterwerte statt absoluter Zaehlerstaende."
- Zustandsbuch je Abschnitt (WLAN assoziiert? Band? Scan-Schalter,
  Bluetooth-Geräte, Stufe, Codec, Sample-Rate).

**Nutzerhandgriffe planen:** Musik starten, Kopfhörer und Telefon an fester
Stelle für beide Arme; WLAN (2,4-GHz-Netz, der Nutzer verbindet selbst) zu
Beginn des einen Arms an, zum anderen aus. Die Reihenfolge der Arme wählst du
und begründest sie. Zeitplan mit Uhrzeiten an den Director, **bevor** der
erste Arm läuft.
**Ablage:** `docs/perf/T-036-trennmessung.md`.

## T-049c — Callback-Probe T-037 (≈20 min, Nutzer: ~10 min)

**Ziel (docs/state.md, wörtlich):** „Callback registrieren, echtes
BQR-Ereignis abwarten, dann **einmalig** pruefen, ob es im Dump noch steht.
Beantwortet die letzte Frage zu AK-7. Braucht Stoerung, deshalb nach der
2,4-GHz-Zelle — und **getrennt**, damit der `dumpsys`-Aufruf T-036 nicht
verfaelscht." Die Störung erzeugt der Nutzer (Vorschlag an ihn mit Dauer).
Ergebnis entscheidet AD-036 (AK-16 „strukturell nicht bestimmbar"): ist der
Pakettyp lesbar?
**Ablage:** `docs/perf/T-037-callback-probe.md`.

## Rückgabe (je Abschnitt)

Kontraktblock plus höchstens dreissig Zeilen; Zahlen mit Pfad und Uhrzeit.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
